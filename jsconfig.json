{
  "$comment": "No build step — this exists purely so editors and agents get type checking and completion from the JSDoc in app.js. `npm run typecheck` runs it.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "checkJs": true,
    "allowJs": true,
    "noEmit": true,
    "strict": false,
    "skipLibCheck": true
  },
  "include": ["app.js"]
}
